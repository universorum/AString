name: CI

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test (${{ matrix.framework }})
    runs-on: ${{ matrix.runs-on }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - framework: netcoreapp2.1
            runs-on: ubuntu-latest
            runtime-dotnet: '2.1.x'
            install-libssl: true
          - framework: netcoreapp3.0
            runs-on: ubuntu-latest
            runtime-dotnet: '3.0.x'
            install-libssl: true
          - framework: net8.0
            runs-on: ubuntu-latest
            runtime-dotnet: '8.x'
          - framework: net10.0
            runs-on: ubuntu-latest
            runtime-dotnet: '10.x'
    steps:
      - uses: actions/checkout@v6
      - name: Install libssl1.1 (required for netcoreapp2.0/3.0 on Ubuntu 22.04)
        if: matrix.install-libssl
        run: |
          wget -q http://archive.ubuntu.com/ubuntu/pool/main/o/openssl/libssl1.1_1.1.1f-1ubuntu2_amd64.deb
          sudo dpkg -i libssl1.1_1.1.1f-1ubuntu2_amd64.deb
      - name: Setup .NET (build SDK)
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.x'
      - name: Setup .NET (test runtime)
        if: matrix.runtime-dotnet != '10.x'
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ matrix.runtime-dotnet }}
      - name: Restore
        run: dotnet restore tests/AString.Tests/AString.Tests.csproj
      - name: Test
        run: >
          dotnet test --project tests/AString.Tests/AString.Tests.csproj
          --configuration Release
          --framework ${{ matrix.framework }}
          --verbosity normal

  pack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.x'
      - name: Restore
        run: dotnet restore
      - name: Build
        run: dotnet build --no-restore --configuration Release
      - name: Pack
        run: >
          dotnet pack AString/AString.csproj
          --no-build
          --configuration Release
          --output ./nupkg
      - name: Upload build output
        uses: actions/upload-artifact@v7
        with:
          name: package
          path: ./nupkg/
          retention-days: 1
  publish:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v') && env.NUGET_API_KEY != ''
    needs: [test, pack]
    env:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
    steps:
      - name: Download build output
        uses: actions/download-artifact@v8
        with:
          name: package
          path: ./nupkg/
      - name: Push to NuGet
        run: >
          dotnet nuget push ./nupkg/*.nupkg
          --api-key "$NUGET_API_KEY"
          --source https://api.nuget.org/v3/index.json
          --skip-duplicate